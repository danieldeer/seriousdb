[project]
name = "seriousdb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "black>=26.5.1",
    "fastapi[standard]>=0.141.1",
    "httpx>=0.28.1",
    "pytest>=8.0.0",
]
